import 'package:flutter/material.dart';

class JusinHomePage extends StatelessWidget {
  const JusinHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Jusin Home Page'),
      ),
      body: const Center(
        child: Text('center'),
      ),
    );
  }
}
